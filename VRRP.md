# VRRP

虚拟路由器冗余技术

​	VRRP组 ———— 需要协同工作的路由器会加入到同一个VRRP组中，一个组需要抽象出一台虚拟的路由器。也需要手工配置一个虚拟的IP地址，还会自动生成一个虚拟的MAC地址

​	简单来说，创建一个虚拟网关，再从多个物理网关中选出一个主设备一个备用设备（当然，这个需要竞争），让虚拟网关附身到主设备上



​	版本：

​		VRRP V2————IPv4————华为默认使用的V2版本

​		VRRP V3————IPv4，IPv6



​	不同的VRRP组需要使用不同的VRID来进行区分和标识——8位二进制构成——取值范围1-255。同一个组中。可以存在两台或多台设备。

​		虚拟的IP地址——配置的虚拟的IP地址必须和物理接口的IP地址在同一个网段中。（路由器网关冗余是这样的）

​		虚拟MAC地址---0000-5e00-01XX——后面8位二进制使用的是VRID。（使用虚拟MAC地址而不是物理接口的MAC地址的原因是，这样可以保证终端设备的ARP缓存表中网关地址和对应MAC地址的对应关系，在主备切换时不会发生变化，用户全程无感知）（现在主备切换时只有接入层的交换机的MAC地址表会改变）





## 具体流程



所有接口激活VRRP之后，都会发送VRRP的数据包，之后比较参数。先比较参数中的优先级（8位二进制构成，1-255，默认值为100。在华为设备中，允许将虚拟的IP地址配置和物理接口IP地址相同的地址，一旦如此物理接口的优先级会变为255，直接变成主设备），优先选择优先级数值大的作为主设备，其余设备为备份设备。如果优先级相同，则比较接口的IP地址，优先选择接口IP地址数值大的作为主设备，其余为备份设备。

之后，仅主设备会周期性（默认1S）的发送VRRP报文，备设备仅侦听，维护一个master-down计时器(3.6S，3X周期时间+偏移时间(256-100/256）），如果在计时器结束前没有收到主设备的VRRP报文，则认为主设备故障，将重新选择新的主设备。



其实就是：

多个设备会互相发送VRRP数据包，先比较优先级，优先级大当主设备，优先级一样就比接口的IP地址，大的当主设备，主设备周期性给其余设备发送VRRP报文以保活，其余设备3.6s没收到则认为主设备挂了，会发送免费ARP告诉交换机













## 配置



<img src="C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260911133222989.png" alt="image-20260911133222989" style="zoom:67%;" />

跳过配IP的步骤



 

```
[R2-GigabitEthernet0/0/1]vrrp vrid 1 virtual-ip 192.168.1.3
配置VRID为1，虚拟IP为192.168.1.3
```



```
[R3-GigabitEthernet0/0/1]vrrp vrid 1 virtual-ip 192.168.1.3
另一个一样的
```

注意，多台需要协同工作的设备VRID和虚拟IP地址都必须相同







```
[R2]display vrrp
查看VRRP
```

![image-20260911134039631](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260911134039631.png)

运行时的优先级，配置优先级，主的优先级



Preempt : YES这个参数说明VRRP默认开启了抢占模式（注意：VRRP的抢占只针对优先级生效。）

这就是为什么后来的1.2没有抢走1.1的主设备的位置





```
[R3-GigabitEthernet0/0/1]vrrp vrid 1 priority 110
修改接口优先级
```

![image-20260911135540633](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260911135540633.png)

可以看到，抢过来了





