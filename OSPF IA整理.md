# OSPF IA整理





获取未知网段路由的方法：

​	1，静态路由---由网络管理员手工添加的路由条目。

​	2，动态路由-- 所有的路由运行相同的路由协议，路由器彼此之间可以相互沟通，交流最终计算出到达未知网段的路由信息。



AS自治系统

​	IGP协议---内部网关协议---RIP，OSPF，IS-IS，EIGRP等

​	EGP协议---外部网关协议---BGP



IGP协议根据算法进行划分：

​	1，距离矢量型协议——贝尔曼福特——直接通过发送路由条目的方式获取未知网段的路由信息——依据传闻的路由协议——RIP

​	2，链路状态型协议——SPF算法——通过传递拓扑信息（LSA---链路状态通告---用来描述周围连接情况）最终获取整个网络的拓扑结构，之后使用SPF算法，将图形接口转换成为树形结构，最终计算出到达未知网段的路由信息。———— OSPF，IS-IS





我们需要一个选路佳，收敛快，占用资源少的IGP协议



RIP --- RIPV1, RIPV2 --- IPV4
		RIPNG --- IPV6
OSPF---OSPFV1（实验室阶段天折），OSPFV2---IPV4
		OSPFV3---IPV6



RIPV2和OSPFV2相同点：

1，RIPV2和OSPFV2一样，都是无类别的路由协议（好事），都支持VLSM和CIDR。——————

2，RIPV2和OSPFV2一样，都是以组播的形式发送传递信息。

​	RIP V2: 224.0.0.9

​	OSPF：224.0.0.5, 224.0.0.6

​		224.0.0.X——本地链路组播---TTL被设置为1（出不了第二个路由器，只会在一个广播域里面）

3，RIPV2和OSPFV2一样，都支持等开销负载均衡。







RIPV2和OSPFV2的区别：

​	RIPV2仅适用于小型的网络环境中，而OSPF适用于中大型的网络环境————OSPF支持结构化部署（区域划分）。

​		如果一个网络规模较大，则可以进行区域划分，形成多区域oSPF网络

​		如果一个网络规模适中，则可以仅包含一个oSPF区域，形成单区域oSPF网络。



OSPF区域划分需要存在一个中间设备-- 区域边界设备-- ABR设备（一个接口对应一个区域）







OSPF区域划分的要求：

​	1，区域之间必须存在ABR设备

​	2，必须按照星型拓扑进行划分——骨干区域---骨干区域的区域ID（区分和标识不同的区域的编号）一定为0，每个区域必须在骨干区域下面（32位二进制构成---1，点分十进制；2，十进制）

星型拓扑结构！ABR设备！



区域划分的核心逻辑--- 区域内部传递拓扑信息，区域之间传递路由信息。————链路状态型协议的距离矢量特征















==**1，OSPF的数据包**==

​	1，hello包---周期性的发现，建立和保活邻居关系。

​		hello时间---10S（30s，网络类型不同导致的）

​		Dead time---4倍的hello时间



​		RID---区分和标识不同路由设备的D- 32位二进制构成

​			1，手工配置---1，IP地址格式进行配置（点分十进制）；2，全网唯一

​			2，自动获取——使用设备的IP地址作为RID，优先选择环回接口的IP地址作为RID，如果存在多个环回接口，则选择环回接口中IP地址数值最大的作为RID；如果没有环回接口，则使用物理接口的IP地址作为RID，如果物理接口存在很多个，则优先选择物理接口中IP地址数值最大的作为RID。（有环回选环回，没换回选直连，选大的）



​	2，DBD包---数据库描述报文---链路状态数据库---LSDB数据库---LSA的信息---仅是描述数据库中LSA的摘要信息。——菜单

​	3，LSR包——链路状态请求报文---根据DBD包，请求自己本地未知的LSA

​	4，LSU包——链路状态更新报文——真正携带LSA的数据包

​	5，LSACK包——链路状态确认报文---确认包



OSPF依然存在周期更新---30min



==**2，OSPF的状态机**==

​	IA的思科的

​	

Exstart状态--- 通过使用未携带数据的DBD包进行主从关系选举。为主的可以优先进行LSA的选择，为从的先进入到下一个状态，发送数据库摘要信息。并且，为主的设备可以主导DBD包的隐形确认。（比较后IP小的变成从设备并且进入到下一个阶段，然后给主设备发送LSA信息，发送的信息的序列号必须是之前主设备用的序列号，代表之前收到主的信息，隐形确认）





Down状态---接口激活，发送hello之后进入到下一个状态;

init状态---当收到邻居的hello包中包含自己本地的RID，则进入到下一个状态；

TWO-WAY状态---标志着邻居关系的建立

（条件匹配）---条件匹配成功，则进入到后面的状态；条件匹配失败，则将停留在邻居关系，通过hello包进行周期保活即可。
Exstart（预启动）状态---通过使用未携带数据的DBD包进行主从关系选举。为主的可以优先进行LSA的选择，为从的先进入到下一个状态，发送数据库的摘要信息。并且，为主的设备可以主导DBD包的隐形确认。
Exchange（准交换）状态---通过发送携带数据的DBD包进行LSA摘要信息的共享

Loading状态--基于DBD包，使用LSR/LSU/LSACK获取未知的LSA信息

FULL状态---标志着邻接关系的建立。

==**3，OSPF的工作过程**==



​	设备激活接口后，会向所有接口以224.0.0.5组播地址为目标地址，发送hello包，如果收到其他设备发送的hello包中存在自己本地的RID，则标志着邻居关系的建立----**邻居表**。

​	之后进行条件匹配，条件匹配失败，则将停留在邻居状态，仅使用hello包进行周期的保活即可。周期时间10s，保活时间（10*4）S.

​	条件匹配成功，则开始建立邻接关系。首先，使用未携带数据的DBD包进行主从关系选举，使用携带数据的DBD包交換LSA的摘要信息。根据DBD包中的信息，使用LSR/LSU/LSACK三种数据获取未知网段的LSA。

​	完成邻接关系的建立---将收集到的LSA放置在本地的LSDB数据库中，生成一张**数据库表——LSDB**

​	OSPF基于收集到的LSA信息构成本地网络的网络拓扑，生成有向图，之后，再使用SPF根据有向图计算出最短路径树，最终，计算出到达未知网段的路由信息。将这些路由信息放在本地的**路由表**中。

​	OSPF收敛结束后，将每隔10s进行一次周期保活。每隔30min进行一次周期更新



​	结构突变：
​		1，突然新增一个网段：触发更新，直接发送携带LSA的LSU包，需要LSACK进行确认。

​		2，突然断开一个网段：触发更新，直接发送携带LSA的LSU包，需要LSACK进行确认。

​		3，无法交流————dead time







==**4，OSPF的基础配置**==



​	看IA的笔记去





如果两端都开启了MTU的检测功能，如果两端的MTu不同，则将卡在Exsatart状态。









开销值：

COST=参考带宽（默认是100Mbps）/真实带宽

注意如果COST计算出来是一个小于1的小数，则视为1；如果是一个大于1的小数，则只取整数部分。

![image-20260914214144531](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260914214144531.png)

修改参考带宽100为1000



