# 路由策略

————针对控制层面流量进行抓取，之后，要么修改其中的参数或者直接不转发该路由信息，影响路由表中路由条目的生成，造成控制选路的效果



和ACL一样，这是在全网做通以后配的，不过之前的ACL限制的是数据层面的流量

而这个路由策略，限制的是控制层面的流量



控制层面的流量————路由协议，为了生成路由表传递的流量

数据层面的流量————访问业务的数据，根据路由表最终到达目标设备的流量







重发布的发布规则————将A协议发布到B协议中时，会将ASBR通过A协议学习到的路由信息，以及宣告在A协议中的接口对应的直连网段信息。

在多点的重发布中，因为种子度量值的原因，所以，必然会出现选路不佳的情况（所以需要用到路由策略来矫正他）





出方向--- 在控制层面流量流出的设备上配置策略，影响其他设备

入方向--- 在控制层面流量流入的设备上配置策略，影响自己。

在路由策略里，**出/入是相对于“本设备的路由表”而言的**：

-   **出方向（Out）**：本设备**向外发布**路由时，对即将发出的路由做过滤或修改。
-   **入方向（In）**：本设备**从邻居接收**路由时，对即将装入自己路由表的路由做过滤或修改。













## 配置



### 抓流量



#### ACL

1，ACL————因为ACL本身是为了抓取数据层流量设计的，其通配符的设计可以灵活的匹配数字特征，但是无法匹配控制层流量中的掩码信息，导致其针对控制层流量时，无法进行精确的抓取。

（比如想抓192.168.1.0/24的路由表的网段，你acl是不是得设置192.168.1.0 0.0.0.255？但是通配符的意思是抓取0位相同的IP，后面什么样它不管，更何况他还有可能抓到192.168.1.0/25这种）



#### 前缀列表

————ip-prefix

**匹配规则：自上而下，逐一匹配，如果匹配上了，则将不再向下匹配。末尾隐含一条拒绝所有的规则。**





```
[r1]ip ip-prefix aa permit 192.168.1.0 24

设置一个名字是aa的抓取（允许）192.168.1.0/24的前缀列表


[r1]ip ip-prefix aa permit 192.168.1.0 25
想加直接写就行了
```





查看一下，这里名字是aa

<img src="C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260924112114448.png" alt="image-20260924112114448" style="zoom:80%;" />

注意：在前缀列表中，规则的序号自动按照10为步调添加（就是这个index）

```
[r1]ip ip-prefix aa index 15 permit 192.168.1.0 26

当然，你也可以自己设置序号
```















有人会说，这一条一条写也太慢了

```
[r1]ip ip-prefix aa permit 192.168.1.0 24 less-equal 28

24小于等于28这个范围，这句相当于把192.168.1.0开头，掩码是24，25，26，27，28的一次性弄进去
```





```
[r1]ip ip-prefix aa permit 192.168.1.0 24 greater-equal 28

这是大于等于，很明显前后矛盾，如果前后矛盾，则后面的为掩码长度（大于等于28，即28，29，30，31，32），前面的为前X位固定（前面24位固定），结合起来就是192.168.1.~/28到32，随便举例子：192.168.1.0/32，192.168.1.128/28
```





```
[r1]ip ip-prefix aa permit 192.168.1.0 16 greater-equal 24 less-equal 24

抓取前16位固定，掩码必须为24
```





```
[r1]ip ip-prefix aa permit 0.0.0.0 0 greater-equal 32

抓取所有掩码长度为32的（主机路由）
```





```
[r1]ip ip-prefix aa permit 0.0.0.0 0 less-equal 32

特殊写法，抓取所有网段
```





```
[r]ip ip-prefix aa permit 0.0.0.0 0

抓取缺省路由
```

有了这些命令，前缀列表就变得非常灵活





### 路由策略





**过滤列表**（filter-policy）

它的作用是**筛选路由**——决定哪些路由能进路由表，哪些不能。

-   前缀列表在这里就是“过滤器”。

-   没匹配上的路由 → **默认拒绝 → 丢弃**。

    

**偏移列表**（offset-list）

它的作用是**修改 metric**——给匹配到的路由加一个跳数，仅此而已。

-   前缀列表在这里只是“挑出要改 metric 的路由”。
-   没匹配上的路由 → **不改 metric，但路由正常通过**。



前缀列表说“你被拒了”，filter-policy理解为“这路由我不要了”，offset-list理解为“这条我不改 metric”。**规则没变，是调用者的解读不同。**





#### rip的Metricin和Metricout

————偏移列表（这个只能针对RIP这类距离矢量型协议进行修改）（偏移列表不会全拒绝）



<img src="C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260924224640450.png" alt="image-20260924224640450" style="zoom:67%;" />

举个例子，看这个图，要是左边想要访问23.0.0.0/24网段，因为种子度量值的原因，会从上面和下面负载均衡，这个时候就需要配置路由策略，在R4上抓取23.0.0.0/24的流量，将他的开销值变大，就可以了



```
[r4]ip ip-prefix aa permit 23.0.0.0 24
偏移列表抓取流量


[r4-GigabitEthernet0/0/0]rip metricout ip-prefix aa 2
修改开销值,因为传递路由是用的R4的0/0/0，出接口，所以写的是metricout，套用名字为aa的偏移列表，开销值的累加值改成2，这样每次加都加2
```

注意：Metircout后面跟的数值代表将RIP数据包中计算开销值使用的累加值从默认的1修改为2（改的累加值）



```
[r4-ac-basic-2000]rule permit source 23.0.0.0 0
ACL抓取流量

[r1-GigabitEthernet0/0/1]rip metricin 2000 2
修改开销值，这次换0/0/1入接口
```

注意：Metricin后面跟的数值代表RIP路由在加表的时候，直接在原先的开销值基础上增加该数值的开销值。（改的最后结果）





#### filter-policy

————过滤策略（过滤列表）



<img src="C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260924224640450.png" alt="image-20260924224640450" style="zoom:67%;" />

还是这个图，这次我R1像访问R4在OSPF的环回4.4.4.4，但是也会上下负载均衡，这次我们用过滤策略，直接过滤掉4.4.4.0的路由，不让他传播，可以在R2的0/0/0出接口和R1的0/0/0入接口配置



```
[r2]ip ip-prefix aa deny 4.4.4.0 24
抓流量

[r2]ip ip-prefix aa permit 0.0.0.0 0 less-equal 32
写一条允许放通所有的规则，防止所有路由被挡下


[r2-rip-1]filter-policy ip-prefix aa export GigabitEthernet 0/0/0
做过滤策略，在R2的RIP端口的0/0/0的出接口上应用名字为aa的前缀列表
```

注意：因为过滤策略本身没有过滤能力，所以，需要在抓取流量的设备，使用deny动作来进行过滤。

注意：因为前缀列表末尾本身隐含了一条拒绝所有的规则，所以，需要将剩余流量放通



```
[rl-acl-basic-2000]rule deny source 4.4.4.0 0.0.0.0
这次用ACL在R1的0/0/0入接口配置过滤策略

[rl-acl-basic-2000]rule permit source any
过滤策略即便是ACL也需要放通所有

[r1-rip-1]filter-policy 2000 import GigabitEthernet 0/0/0
做过滤策略，在R1的RIP端口的0/0/0的入接口上应用序号为2000的ACL
```









#### 路由策略

————ROUTE-POLICY



**匹配规则：自上而下，逐一匹配，如果匹配上了，则将不再向下匹配。末尾隐含一条拒绝所有的规则。**

<img src="C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260925193102057.png" alt="image-20260925193102057" style="zoom:67%;" />

左边的五个网段需要做出不同的效果：

​	拒绝接收1.0.0.0/8网段

​	接收2.0.0.0/8类型时把Cost开销值类型改为1

​	接收3.0.0.0/8网段时把其种子度量值改成2

​	接收4.0.0.0/8网段时把Cost开销值类型改为1，tag标记改为666

```
[R2-acl-basic-2000]rule permit source 2.0.0.0 0
[R2-acl-basic-2001]rule permit source 1.0.0.0 0
[R2]ip ip-prefix aa permit 3.0.0.0 8
[R2]ip ip-prefix bb permit 4.0.0.0 8
先抓流量，因为每条抓到之后的操作不同，所以需要用四条ACL或者前缀列表抓

[R2]route-policy xgz deny node 10
创建一个名字为xgz的路由策略表，序号为10，操作为拒绝的，还没有对应抓取方式，
[R2-route-policy]if-match acl 2001
将ACL 2001作为xgz路由策略的抓取规则（没有apply，所以执行大动作，就是上面的拒绝）（小细节：即便我是要拒绝，ACL写的也是permit）

[R2]route-policy xgz permit node 20
继续在名字为xgz的路由策略里创建序号为20的，操作为允许的，还没有对应抓取方式
[R2-route-policy]if-match acl 2000 
[R2-route-policy]apply cost-type type-1
按照ACL 2000抓取流量，把Cost类型改成1

[R2]route-policy xgz permit node 30
继续在名字为xgz的路由策略里创建序号为30的，操作为允许的，还没有对应抓取方式
[R2-route-policy]if-match ip-prefix aa
[R2-route-policy]apply cost 2
按照前缀列表aa抓取流量，把Cost种子度量值改成2

[R2]route-policy xgz permit node 40
继续在名字为xgz的路由策略里创建序号为40的，操作为允许的，还没有对应抓取方式
[R2-route-policy]if-match ip-prefix bb
[R2-route-policy]apply cost-type type-1
[R2-route-policy]apply tag 666
按照前缀列表bb抓取流量，把Cost类型改成1，tag标记改为666

[R2]route-policy xgz permit node 50
再写一条允许所有的规则




[R2-ospf-1]import-route rip 1 route-policy xgz 
重发布的时候调用路由策略xgz
```

注意：路由策略的规则序号是手工添加的，建议也按照10为步调添加，方便插入规则。

注意：

​	1，即使要拒绝一个路由，在抓取流量时，动作依然选择允许，之后，在路由策略中拒绝。

​	2，在一条规则中，没有if-match，则代表匹配所有；如果没有apply，则仅执行大动作。

​	3，规则和规则之间按照“或”的关系执行；但是一条规则中包含多个apply或if-match，他们之间按照“与”的关系执行。（多个if-match需要同时满足才会执行）



<img src="C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260925184417253.png" alt="image-20260925184417253" style="zoom: 67%;" />

能改的东西还不少哩







结果：

<img src="C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260925192354469.png" alt="image-20260925192354469" style="zoom:67%;" />

挺成功的



