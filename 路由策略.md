# 路由策略

————针对控制层面流量进行抓取，之后，要么修改其中的参数或者直接不转发该路由信息，影响路由表中路由条目的生成，造成控制选路的效果



和ACL一样，这是在全网做通以后配的，不过之前的ACL限制的是数据层面的流量

而这个路由策略，限制的是控制层面的流量



控制层面的流量————路由协议，为了生成路由表传递的流量

数据层面的流量————访问业务的数据，根据路由表最终到达目标设备的流量







重发布的发布规则————将A协议发布到B协议中时，会将ASBR通过A协议学习到的路由信息，以及宣告在A协议中的接口对应的直连网段信息。

在多点的重发布中，因为种子度量值的原因，所以，必然会出现选路不佳的情况（所以需要用到路由策略来矫正他）





出方向--- 在控制层面流量流出的设备上配置策略，影响其他设备

入方向--- 在控制层面流量流入的设备上配置策略，影响自己。

在路由策略里，**出/入是相对于“本设备的路由表”而言的**：

-   **出方向（Out）**：本设备**向外发布**路由时，对即将发出的路由做过滤或修改。
-   **入方向（In）**：本设备**从邻居接收**路由时，对即将装入自己路由表的路由做过滤或修改。













## 配置



### 抓流量



#### ACL

1，ACL————因为ACL本身是为了抓取数据层流量设计的，其通配符的设计可以灵活的匹配数字特征，但是无法匹配控制层流量中的掩码信息，导致其针对控制层流量时，无法进行精确的抓取。

（比如想抓192.168.1.0/24的路由表的网段，你acl是不是得设置192.168.1.0 0.0.0.255？但是通配符的意思是抓取0位相同的IP，后面什么样它不管，更何况他还有可能抓到192.168.1.0/25这种）



#### 前缀列表

————ip-prefix



```
[r1]ip ip-prefix aa permit 192.168.1.0 24

设置一个名字是aa的抓取（允许）192.168.1.0/24的前缀列表


[r1]ip ip-prefix aa permit 192.168.1.0 25
想加直接写就行了
```





查看一下，这里名字是aa

<img src="C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260924112114448.png" alt="image-20260924112114448" style="zoom:80%;" />

注意：在前缀列表中，规则的序号自动按照10为步调添加（就是这个index）

```
[r1]ip ip-prefix aa index 15 permit 192.168.1.0 26

当然，你也可以自己设置序号
```







匹配规则：自上而下，逐一匹配，如果匹配上了，则将不再向下匹配。末尾隐含一条拒绝所有的规则。







有人会说，这一条一条写也太慢了

```
[r1]ip ip-prefix aa permit 192.168.1.0 24 less-equal 28

24小于等于28这个范围，这句相当于把192.168.1.0开头，掩码是24，25，26，27，28的一次性弄进去
```





```
[r1]ip ip-prefix aa permit 192.168.1.0 24 greater-equal 28

这是大于等于，很明显前后矛盾，如果前后矛盾，则后面的为掩码长度（大于等于28，即28，29，30，31，32），前面的为前X位固定（前面24位固定），结合起来就是192.168.1.~/28到32，随便举例子：192.168.1.0/32，192.168.1.128/28
```





```
[r1]ip ip-prefix aa permit 192.168.1.0 16 greater-equal 24 less-equal 24

抓取前16位固定，掩码必须为24
```





```
[r1]ip ip-prefix aa permit 0.0.0.0 0 greater-equal 32

抓取所有掩码长度为32的（主机路由）
```





```
[r1]ip ip-prefix aa permit 0.0.0.00 less-equal 32

特殊写法，抓取所有
```







### 路由策略

