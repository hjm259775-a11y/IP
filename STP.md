# STP

生成树协议，一个运行在交换机间的协议



首先我们先看下这个图：

![image-20260907204029464](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260907204029464.png)

三个交换机成环，在泛洪时会：

1，广播风暴 ———— 在环路中，可能出现顺时针和逆时针两重无限循环的流量，不断消耗设备性能资源，最终导致网络瘫痪，设备宕机。

2，MAC地址表翻摆 ———— MAC地址表内容不停的发生变化，消耗设备的硬件性能。

3，多帧复制 ———— 接入交换网络的设备会大量重复收到相同的数据帧，消耗设备的性能。





重点来了，这个STP就能让交换机在这种情况下还能破除环路

！？强强？！





大致背景也可以了解下：

​	802.1D标准——STP——生成树协议，这是公共协议

​	思科对其进行改进，有了PVST，PVST+协议，思科私有协议

​	而后出现 802.1W——RSTP——快速生成树（公共）

​	还有 802.1S——MSTP——多生成树（公共）







## 思路

​	**在二层交换网络中，逻辑上阻塞部分接口，实现从根交换机到达各个节点唯一的最短路径，生成一个没有环路的拓扑。之后，当最佳选路出现故障时，可以将个别阻塞端口放通，形成备份路径**







## 流程



交换机和交换机之间发送的数据包————BPDU，这是一个跨三四层封装的数据包

​	**一，配置BPDU——用来生成，维护树形结构**

![image-20260907213130826](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260907213130826.png)

1，

2，协议版本ID————802.1D为0，802.1W---RSTP---1，802.1S ---MSTP---2

3，

4，FLAG---8位---在8021D中，仅使用了最高位和最低位（TC和TCA位），中间的6位在802.1W之后启用

==**5**==，根网桥ID---为根的交换机的BID就是根网桥ID（时刻提醒着老大是谁）

==**6**==，RPC——根路径开销---发出该BPDU设备到达根网桥的开销值。（到老大那要花多少资源）

​	生成树的开销值也是赋予在接口上的，也和接口的带宽有关，根路径开销指发出该BPDU到达根网桥过程中接口开销的累加值。

​	STP中接口开销值是按照规则表对照出来的，对照的标准常用的有三个标准，华为默认使用的802.1t标准。

​	![image-20260907220840822](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260907220840822.png)

![image-20260907221408421](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260907221408421.png)

RPC的计算方法---根网桥发出的RPC为0。其他设备发出的RPC为接收到的RPC加上进入接口的开销值



==**7**==，网桥ID-- 8字节---用来区分和标识不同的交换机（交换机自己的身份证）

​	前2字节——优先级

​	后6字节——MAC地址

​	如果交换机只有一个MAC地址，则直接使用该MAC地址；如果交换机存在多个MAC地址，则选择数值较小的作为BID中的MAC地址



==**8**==，接口ID--PID--2字节---用来区分和标识交换机不同的接口，便于之后选出来进行阻塞（接口的身份证）

​	前4位--优先级

​	后12位---接口的编号

9，消息寿命---根网桥发出时，该参数为0，之后，没经过一个设备转发，参数加1，存在最大限制MAX-HOP为20跳，超出则认为不可达

10，

11，Hello时间——2s——只有老大每隔2s才会主动发，当然，这并不代表别的交换机收到消息后不会发，只是需要这个契机

12，





**二，TCN BPDU——如果拓扑结构发生变化，则需要这个来上报信息**

![image-20260907230641681](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260907230641681.png)

因为这个东西只要一发出来，就知道拓扑发生变化了，所以并不包含拓扑信息，况且交换机的MAC地址表也没法手动更改



### TCN应对拓扑变化



拓扑结构发生变化的时候，当事设备会从其他所有接口泛洪TCN BPDU，告知其他设备拓扑变更了。其他设备接收到后，将回复一个配置BPDU其中TCA位置1，代表确认（这个配置BPDU不是主动转发，是转发的根网桥的配置BPDU）。之后，再逐级上传，最终到达根网桥。根网桥确认信息后，发送TC标记位置为1 的配置BPDU，并逐级泛洪到所有设备。其他设备收到TC置1的配置BPDU后，会临时将MAC地址表300s修改为15s，加快MAC地址表错误信息的清除。





![image-20260908114756668](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260908114756668.png)

如图所示：

​	当右上角链路断开之后，最先发现拓扑改变的SWC（当事设备）会用逻辑堵塞的链路发1.TCN

​	SWB收到TCN之后，会返回一个2.TCA，这是一个配置BPDU（当然这是老大给的，小弟只转发），里面标识位的TCA置为1

​	之后SWB会向SWA发送TCN，相当于把TCN转发出去

​	SWA老大收到后会主动发送配置BPDU，此时里面标识位的TC置为1（发送出去代表老大已经知道拓朴发生变化了，让小弟的MAC地址表加速老化，清除错误信息）

​	之后像SWB这样的小弟也会转发这个老大发来的配置BPDU





效率是慢了点，不过确实是这么个逻辑

## 具体流程（文本）



### 网络起始阶段

![image-20260907224739641](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260907224739641.png)

在交换网络初始阶段，所有的交换机都会认为自己是老大（只有老大才会主动发消息），所以会向自己的所有端口发送配置BPDU

之后比较参数，基于参数选举出老大是LW1，之后就是LW1每隔2s发配置BPDU，小弟只会转发这个老大发的配置BPDU

​	举例LW1给LW2的配置BPDU：

​		根网桥ID：LW1 ； 网桥ID：LW1 ； RPC：0 ； 接口ID：0/0/1

​	LW2收到之后会进行转发，举例LW2给LW4的：

​		根网桥ID：LW1 ； 网桥ID：LW2； RPC：0+LW2的0/0/1开销 ； 接口ID：0/0/3



一旦20s没有收到，就会认为老大没了，就会认为自己是老大，又会主动的发配置BPDU，然后选举老大





### 生成树角色选举

根网桥——RB——802.1D中，一个交换网络仅生成一颗树
根端口——RP——每一台非根网桥有且仅有一个离根最近的端口作为根端口，用来接收根网桥的配置BPDU
指定端口——DP——每一根链路上选择一个端口作为指定端口，用来发送或者转发根网桥的配置BPDU
非指定端口——NDP——需要被阻塞的端口

![image-20260908135635581](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260908135635581.png)

看个图就懂了，非指定端口是要堵塞的













## 配置













